#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  network:
    version: 2
    ethernets:
      en-default:
        match:
          name: "en*"
        dhcp4: true
  storage:
    layout:
      name: direct
  identity:
    hostname: vagrant-ubuntu2404
    username: vagrant
    password: $6$rounds=4096$8dkK1P/oE$L1L.e4qH7QMH9yv6lCB6VVFZvdvYdl5QmKmB5yLiXLCYLGJjR7X5R4eBPEPOPXHhSjuFPQ1NNlMDTDXGdCDDT0
  ssh:
    install-server: true
    allow-pw: true
  packages:
  - openssh-server
  - cloud-init
  - net-tools
  - curl
  - wget
  user-data:
    disable_root: false
    timezone: UTC
  late-commands:
  - echo 'vagrant ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/vagrant
  - chmod 440 /target/etc/sudoers.d/vagrant
  - curtin in-target --target=/target -- sed -i 's/^#PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config
  - curtin in-target --target=/target -- sed -i 's/^#PubkeyAuthentication.*/PubkeyAuthentication yes/' /etc/ssh/sshd_config
  - curtin in-target --target=/target -- sed -i 's/^PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config
